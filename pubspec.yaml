name: edu_sign
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.1.1
  firebase_auth: ^6.1.0
  http: ^1.2.0
  # youtube_player_flutter: 9.1.3
  youtube_player_iframe: ^5.1.2
  flutter_inappwebview: ^6.0.0
  auto_size_text: ^3.0.0
  video_player: ^2.9.1
  youtube_explode_dart: ^2.0.0
  
  cupertino_icons: ^1.0.8
  cloud_firestore: ^6.0.2
  cloud_functions: ^6.0.0  # <-- INI DIA PERBAIKANNYA
  image_picker: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  assets:
    - assets/images/logo.png

  uses-material-design: true